language: python
python:
  - "2.7"
install:
  - pip install -r requirements.txt
jobs:
  include:
    - stage: check
      script: ./runcheck
    - stage: test
      script: source ./settestenv && ./runtests
after_success:
  - codecov